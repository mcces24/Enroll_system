<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Handle root and subdirectories recursively
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.+)$ $1.php [L,QSA]
</IfModule>
